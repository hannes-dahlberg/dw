<?php
namespace dw;

class Ban extends Core {
	/*
		Checking if a ip, email or string is in the database and return true if so.
		If combined with multiple parameters all of them have to be true
	*/
	static function check($params) {
		
	}
	
	static function add($params) {
		
	}
}